name: Terraform Destroy

on:
  workflow_dispatch:
    inputs:
      confirm_destroy:
        description: 'Type "destroy" to confirm destruction of all infrastructure'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

defaults:
  run:
    working-directory: terraform

jobs:
  destroy:
    name: Destroy
    runs-on: ubuntu-latest
    if: inputs.confirm_destroy == 'destroy'
    steps:
      - uses: actions/checkout@v6

      - uses: hashicorp/setup-terraform@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::637423429740:role/terraform-github-actions
          aws-region: us-east-1

      - name: terraform init
        run: terraform init

      - name: Capture VPC ID before destroy
        id: vpc
        run: echo "id=$(terraform output -raw vpc_id)" >> "$GITHUB_OUTPUT"

      - name: Check for CloudShell VPC environments (must be deleted manually)
        run: |
          set -euo pipefail
          VPC_ID="${{ steps.vpc.outputs.id }}"

          # CloudShell VPC envs create service-owned ENIs that block subnet/SG/VPC
          # delete. AWS does not expose a CLI/SDK to manage VPC environments —
          # they must be removed via the AWS Console before destroy can succeed.
          CLOUDSHELL_ENIS=$(aws ec2 describe-network-interfaces \
            --filters "Name=vpc-id,Values=$VPC_ID" \
                      "Name=description,Values=*CloudShell*" \
            --query 'NetworkInterfaces[].NetworkInterfaceId' \
            --output text)

          if [ -n "$CLOUDSHELL_ENIS" ]; then
            echo "::error::CloudShell ENIs still present in $VPC_ID: $CLOUDSHELL_ENIS"
            echo "::error::Delete the CloudShell VPC environment(s) before re-running destroy:"
            echo "::error::  AWS Console → CloudShell → Actions → Manage VPC environments → Delete"
            exit 1
          fi

          echo "No CloudShell ENIs in $VPC_ID — safe to proceed"

      - name: Delete available ENIs (Lambda/EKS orphans)
        run: |
          VPC_ID="${{ steps.vpc.outputs.id }}"
          AVAILABLE_ENIS=$(aws ec2 describe-network-interfaces \
            --filters "Name=vpc-id,Values=$VPC_ID" "Name=status,Values=available" \
            --query 'NetworkInterfaces[*].NetworkInterfaceId' \
            --output text)
          for ENI_ID in $AVAILABLE_ENIS; do
            echo "Deleting orphaned ENI: $ENI_ID"
            aws ec2 delete-network-interface --network-interface-id "$ENI_ID" || \
              echo "Skipping $ENI_ID (likely service-owned)"
          done

      - name: Destroy EKS and dependent resources first
        run: |
          terraform destroy \
            -target=module.monitoring \
            -target=aws_security_group_rule.cloudshell_to_eks \
            -target=module.eks \
            -var-file=prod.tfvars \
            -auto-approve

      - name: Wait for EKS ENIs to be released
        run: |
          VPC_ID="${{ steps.vpc.outputs.id }}"
          echo "Waiting for all ENIs in VPC to clear..."
          for i in $(seq 1 30); do
            COUNT=$(aws ec2 describe-network-interfaces \
              --filters "Name=vpc-id,Values=$VPC_ID" \
              --query 'length(NetworkInterfaces)' \
              --output text)
            echo "Attempt $i/30: $COUNT ENIs remaining"
            [ "$COUNT" -eq 0 ] && echo "All ENIs cleared" && break
            [ "$i" -eq 30 ] && echo "ERROR: Timed out waiting for ENI cleanup" && exit 1
            sleep 20
          done

      - name: Destroy remaining resources
        run: terraform destroy -var-file=prod.tfvars -auto-approve
